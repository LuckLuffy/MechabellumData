# MechabellumData 项目复盘

> 目标：从 Mechabellum（钢铁指挥官）游戏中提取兵种单位的原始属性数据  
> 游戏版本：1.11.1.1.2207 | Unity 2022.3 + IL2CPP + Addressables  
> 时间跨度：2026-07-28 ~ 2026-07-29  
> 实际耗时：约 8 轮对话

---

## 一、项目目标

从游戏文件中提取每类兵种的**精确基础属性**（生命值、攻击力、移动速度、费用等），而非依赖网络 wiki（wiki 域名已被劫持/数据过时）。

---

## 二、尝试过的方案

### 方案 1：网络搜索 ← 快速但不可靠

**做法**：通过搜索引擎抓取 mechabellum.wiki 和 mechamonarch.com 的缓存数据。

**结果**：
- 获取了约 26/30 单位的属性数据
- mechabellum.wiki 域名已被劫持为赌博网站
- 数据来自 v0.7~v1.0 旧版本，当前版本 1.11 已有大量调整
- 4 个单位数据完全缺失（Sandworm, Raiden, PhantomRay, Feed）

**教训**：在线数据源不可靠。游戏迭代快，社区 wiki 可能随时消失。

---

### 方案 2：Il2CppDumper 静态分析 ← 成功的核心步骤

**做法**：用 Il2CppDumper v6.7.46 解析 `global-metadata.dat`（23MB）+ `GameAssembly.dll`（69MB）。

**产出文件**：

| 文件 | 大小 | 价值 |
|------|------|------|
| `dump.cs` | 38MB | 完整 C# 类型定义（所有类/字段/方法） |
| `script.json` | 113MB | JSON 格式结构信息 |
| `stringliteral.json` | 2.3MB（103K行） | 全部字符串常量 |
| `il2cpp.h` | 61MB | IL2CPP 编译后的 C++ 内存布局 |
| `DummyDll/` | 80+ DLL | 可引用的类型存根 |

**关键发现**：
```
MechData  : life(int) + damage(int) + moveSpeed(int) + isFly(bool) + ...
CardData  : baseMoney(int) + unlockPrice(int) + mechCount(int) + ...
ConfigDataContainer : mechDatas(List<MechData>) + cardDatas(List<CardData>)
```
- 确定了完整的 C++ 内存布局（字段偏移已验证正确）
- 发现了 Config 单例的访问路径：`Config.Instance → dataContainer → mechDatas/cardDatas`

**局限**：Il2CppDumper 只提取**类型结构**，不提取**实例数据**。实际的 HP/ATK 等数值存储在 Unity ScriptableObject 中，需要其他工具。

---

### 方案 3：UnityPy 解析 Bundle ← 结构可见但数值不可达

**做法**：用 Python UnityPy 库加载游戏的全部 109 个 bundle/assets 文件。

**扫描结果**：

| 文件 | 大小 | 内容 |
|------|------|------|
| `common_assets_all.bundle` | 89MB | 1 个 VideoClip + AssetBundle 元数据 |
| `scuffle_assets_all.bundle` | 596MB | 100 SkinData + 212 Texture + 105 Material |
| `mechshow_assets_all.bundle` | 313MB | 10,634 个视觉对象（模型/相机/特效） |
| `sharedassets0~20.assets` | 数 GB | UI 预制体、粒子系统、动画 |
| `resources.assets` | 20KB | 267 MonoBehaviour + 142 TextAsset（Sprite图集JSON + 敏感词库） |

**发现**：
- 109 个文件中**没有任何 ScriptableObject 或 ConfigData 类型**被识别
- 所有 MonoBehaviour 均为视觉/UI 组件
- 二进制搜索确认 `int(263)`（Crawler HP）在 scuffle_assets 中出现 280 次 → 数据确实存在
- 但 UnityPy 无法遍历 CAB（Compressed Asset Bundle）嵌套容器

**局限**：UnityPy 对 Addressables CAB 压缩容器的支持有限，无法解析嵌套的 ScriptableObject。

---

### 方案 4：AssetRipper 解析导出 ← 能导出但不含数据

**做法**：下载 AssetRipper v1.3.14（41MB Web GUI 工具），加载游戏文件并导出。

**结果**：
- `common_assets_all.bundle` → 导出 232 个文件（87MB），**全为视觉资源**（Mesh/Texture/Shader/Video），无 ScriptableObject
- `resources.assets` → 导出 13,742 个文件（3.5GB），含 TextAsset 但均为 UI 图集 JSON 和本地化数据
- 导出文件数是加载文件的数十倍（AssetRipper 递归导出所有依赖资源）
- 中文界面切换导致崩溃（已知 bug）

**局限**：AssetRipper 导出的资源不含 MechData/CardData 实例。这些数据可能在 `.assets.resS` 文件中（Unity Resource Serialization，无法被 AssetRipper 加载），或从服务器下载。

---

### 方案 5：pymem 内存扫描 ← 理论可行但太慢

**做法**：Python pymem 附加游戏进程，扫描 5.5GB 已提交内存。

**结果**：
- 全量扫描极慢（30 分钟未完成）
- 精确定位需要 IL2CPP 运行时类指针 → 复杂度过高

---

### 方案 6：Cheat Engine 内存扫描 ← 工具快但数据定位失败

**做法**：CE 7.5 附加游戏，AOB/组搜索 Crawler 特征码 [HP=263, ATK=79, SPD=16]。

**结果**：
- CE `memscan` 搜单值 263 → 96 个地址但都不在 MechData 附近
- AOB 扫描特征码 → 0 个匹配
- IL2CPP 编译后的内存布局与 dump.cs 偏移完全一致（已通过 `il2cpp.h` 验证）
- 失败原因：MechData 配置对象在游戏运行时的内存位置和可扫描性未知

---

## 三、核心障碍总结

```
游戏数据流：
  Unity ScriptableObject（编辑器中）
    → IL2CPP 编译
    → ???（未知存储格式）
    → Config.Init() 加载
    → ConfigDataContainer.mechDatas（运行时内存）
```

**根本问题**：MechData/CardData 的实例数据在游戏文件中的确切位置和格式，经过 5 种工具/方法尝试后仍未能定位。可能的原因：

1. **CAB 压缩容器**：Addressables 使用 CAB 格式压缩 ScriptableObject，UnityPy 和 AssetRipper 均无法完整遍历
2. **服务器下发**：Protobuf 版 CardData 定义表明数据可能通过网络传输，本地没有完整副本
3. **加密/自定义序列化**：GameAssembly.dll 中未找到标准 int32 编码的数值，暗示可能存在自定义编码

---

## 四、已产出资产

| 文件 | 说明 |
|------|------|
| `游戏原始数据解析.md` | 16.6KB，完整的数据模型、ID 列表、存储位置、服务器 API |
| `开发文档.txt` | 8.2KB，6 轮对话的完整记录 |
| `tools/dump.cs` | 38MB，全部 C# 类型定义 |
| `tools/script.json` | 113MB，JSON 结构信息 |
| `tools/stringliteral.json` | 2.3MB，全部字符串常量 |
| `tools/il2cpp.h` | 61MB，C++ 内存布局 |
| `tools/Il2CppDumper/` | Il2CppDumper 可执行文件 |
| `tools/AssetRipper/` | AssetRipper 可执行文件 |
| `Mechabellum_Unit_Data.CT` | CE 作弊表（含 Lua 自动扫描脚本） |
| `extract_unit_data.py` | Python pymem 扫描脚本（框架） |
| `scan_memory.py` | Python 堆扫描脚本 |
| `extract_with_ripper.py` | Python AssetRipper API 脚本 |
| `操作指南.md` | CE 操作指南 |

---

## 五、未被尝试但有希望的方向

### 5.1 Cpp2IL + Il2CppInspector（工具链组合）
- Cpp2IL 比 Il2CppDumper 支持更多 IL2CPP 版本
- Il2CppInspector 提供 GUI，可浏览类型、字段和**静态字段值**

### 5.2 BepInEx 6.x IL2CPP 插件
- 直接在游戏进程中运行 C# 代码
- 调用 `Config.Instance.GetNormalConfigDatas()` 即可获取全部数据
- 需要 BepInEx 6.x Bleeding Edge（支持 IL2CPP）

### 5.3 网络抓包
- 游戏从 `mechabellumgame.com` 下载大量配置
- 用 Fiddler/Charles 抓取 Protobuf 响应可能直接包含单位数据
- 已确认 CardData 有 Protobuf 定义

### 5.4 运行时 DLL 注入
- 注入自定义 DLL 到游戏进程
- 通过 IL2CPP 类指针访问 ConfigDataContainer
- 用 Il2CppDumper 输出的 RVA 定位目标函数

### 5.5 Unity 资源格式深度解析
- `.assets.resS` 文件（最大 2GB）包含资源数据
- 需要专门的 UnityFS/SerializedFile 解析器

---

## 六、经验教训

1. **Il2CppDumper 是 IL2CPP 游戏分析的基石**：类型结构、字段偏移、字符串表一应俱全
2. **Unity Addressables 是反编译的噩梦**：CAB 压缩 + 模糊命名（hex 哈希）使数据追踪极其困难
3. **AssetRipper 不适合大型游戏**：递归导出所有依赖导致体积爆炸（20KB → 3.5GB）
4. **CE 内存扫描对非 Mono 游戏效果有限**：IL2CPP 堆结构难以从外部遍历
5. **网络数据源不可靠**：社区 wiki 可能随时消失或过时
6. **工具链比蛮力更重要**：一次成功的数据提取需要 Il2CppDumper + UnityPy + AssetRipper 的串联使用，缺一不可

---

*生成时间：2026-07-29 | 项目目录：D:\Desktop\Program\MechabellumData*
