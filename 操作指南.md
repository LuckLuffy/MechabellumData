# Mechabellum 单位数据提取 — 操作指南

## 环境

- 游戏：Mechabellum v1.11.1.1.2207（IL2CPP）
- 工具：Cheat Engine 7.5（`D:\wyq\Cheat Engine 7.5`）
- 数据表：`Mechabellum_Unit_Data.CT`

---

## 一、自动化提取（推荐，1-2分钟）

> 前提：游戏已进入 **训练模式** 并部署了至少 1 个 Crawler（爬虫）

### 步骤

1. **启动 CE 并附加进程**
   - 运行 `D:\wyq\Cheat Engine 7.5\cheatengine-x86_64.exe`
   - 点击闪烁的电脑图标 → 选择 `Mechabellum.exe` → Open

2. **加载 CT 表**
   - `File` → `Load` → 选择 `Mechabellum_Unit_Data.CT`
   - 或直接拖拽 CT 文件到 CE 窗口

3. **执行 Lua 脚本**
   - 展开 `[Auto] 扫描单位数据` → 点击 `[Lua] 自动扫描` 条目
   - 点击右侧的 **`Execute Script`** 按钮
   - 等待 30-60 秒，结果输出在 Lua Engine 窗口

4. **复制结果**
   - CE 菜单 `Table` → `Show Lua Engine`
   - `Ctrl+C` 复制所有单位数据

### 原理

脚本用 AOB（Array of Bytes）在内存中搜索 Crawler 的特征码：

```
life=263, damage=79, speed=16, isFly=0
→ 07 01 00 00  4F 00 00 00  10 00 00 00  00 00 00 00
```

找到后逆推出 `MechData` 结构地址，再反查包含该指针的 `mechDatas` 数组，一次性读取全部 30+ 单位。

---

## 二、半自动提取（如果步骤一失败）

如果 AOB 扫描找不到结果（Crawler 数值可能有版本差异），改为手动搜索：

### 步骤

1. **训练模式部署 Crawler**
   - 当前 Crawler：HP=263，ATK=79，SPD=16
   - 如果不确定，先看游戏内属性面板确认

2. **CE 搜索 HP**
   - Value Type: `4 Bytes`
   - Scan Type: `Exact Value`
   - 输入 `263` → `First Scan`
   - 给 Crawler 升 1 级（HP 变化）
   - 输入新 HP → `Next Scan`
   - 重复直到只剩 1-3 个地址

3. **浏览内存**
   - 右键找到的绿色地址 → `Browse this memory region`
   - 你会看到 HP 值附近有 ATK(79) 和 SPD(16)
   - 此时光标所在位置是在 `FightMech` 运行时对象中

4. **找到 MechData 指针**
   - 在内存窗口中，HP 地址前约 `0xB0` 字节处找 8 字节指针
   - 该指针指向 `MechData` 基址
   - 或者：直接在找到的 HP 地址上右键 → `Find out what accesses this address`
   - 在战斗过程中会看到代码读取该地址，从而追踪

5. **从 MechData 反查列表**
   - 记下 MechData 基址（例如 `0x19600AABBCC`）
   - 在 CE 中：`New Scan` → `Array of Bytes` → 输入该地址的十六进制
   - 搜索包含该指针的内存位置
   - 找到指针数组 → 即 `mechDatas._items` → 遍历全部单位

---

## 三、手动逐个提取（兜底方案，15-20分钟）

1. **逐单位部署 + 搜索 HP**
   - 部署单位 → CE 搜索 HP → 浏览内存 → 读取相邻的 ATK 和 SPD
   - 当前已知值范围：
     - HP: 100 ~ 150,000
     - ATK: 20 ~ 14,000
     - SPD: 6 ~ 16

2. **内存布局参考**（在找到的 HP 地址处）：

```
     HP地址 - 0x38 ~ 0x00 ：MechData 其他字段
     -0x38 ：life     (int)  ← HP 在这里
     -0x34 ：damage   (int)  ← ATK 在上方4字节
     -0x30 ：moveSpeed(int)  ← 速度在上方8字节
     +0x2C ：mechType (int)  ← 0小 1中 2巨
     +0x30 ：moveType (int)  ← 0普通 1潜地 2隐形
```

---

## 四、数据结构速查表

### MechData（战斗属性）

| 偏移 | 类型 | 字段 |
|------|------|------|
| `+0x38` | int32 | **life** — 生命值 |
| `+0x3C` | int32 | **damage** — 攻击力 |
| `+0x40` | int32 | **moveSpeed** — 移动速度 |
| `+0x44` | int32 | **isFly** — 0=地面 1=飞行 |
| `+0x48` | int64 | attackAngle (FPoint) |
| `+0x50` | int64 | rotateSpeed (FPoint) |
| `+0x60` | int32 | **mainSkillID** — 主武器ID |
| `+0x64` | int32 | **mechType** — 0=Small 1=Medium 2=Huge |
| `+0x68` | int32 | **moveType** — 0=Normal 1=Underground 2=Cloak |
| `+0x90` | int32 | attackStrength — 对建筑伤害 |
| `+0xA0` | int64 | radius (FPoint) |

### CardData（卡片/UI数据）

| 偏移 | 类型 | 字段 |
|------|------|------|
| `+0x58` | int32 | **baseMoney** — 购买费用 |
| `+0x5C` | int32 | **maintenanceSupply** — 维护费 |
| `+0x80` | int32 | **mechID** — 单位ID |
| `+0x84` | int32 | **mechCount** — 每队数量 |
| `+0x88` | int32 | **slotSize** — 占用槽位 |
| `+0xA0` | int32 | **unlockPrice** — 解锁费 |
| `+0xC0` | int32 | specialUnit — 特殊标记 |

### 枚举值

**UnitType (mechType)**：
- `0` = Small（小型：Crawler, Fang, Arclight, Marksman, Hound 等）
- `1` = Medium（中型：Mustang, Steel Ball, Rhino, Phoenix, Wasp 等）
- `2` = Huge（巨型：Fortress, Vulcan, Melting Point, Overlord, War Factory, Abyss）

**MechMoveType (moveType)**：
- `0` = Normal（普通移动）
- `1` = Underground（潜地移动，如 Sandworm）
- `2` = Cloak（隐形移动，如 Wraith）

---

## 五、故障排除

| 问题 | 解决方案 |
|------|---------|
| CE 找不到进程 | 以**管理员身份**运行 CE |
| AOB 扫描无结果 | 确认已部署 Crawler；用方案二手动搜索 HP |
| Lua 脚本报错 | 检查 CE 版本是否为 7.5；Lua Engine 窗口查看错误详情 |
| HP 搜索后地址太多 | 多升级几次让 HP 变化更大；锁定 Value Type 为 4 Bytes |
| 游戏崩溃 | CE 只读不作写操作不会导致崩溃；如有问题重启游戏即可 |

---

## 六、输出示例

成功执行后，Lua Engine 窗口将输出类似：

```
游戏版本 1.11.1.1
======================================================================
单位数据 (共 31 个)
======================================================================
ID     HP      ATK     SPD  飞行  体型      移动方式     技能ID
----------------------------------------------------------------------
 0     263       79     16    -   Small     Normal       1001
 1     117       61      6    -   Small     Normal       1002
 2    4414      391      7    -   Small     Normal       1003
 3    1622     2329      8    -   Small     Normal       1004
...
28  146782    14368      6    -   Huge      Normal       1028
29   78310    38586     10    ✈   Huge      Normal       1029
30    null     null    null  ...                                             
```

- 单位 0-30 对应标准单位（与 GameData.grdb 中的 unitID 按列表顺序对应）
- `✈` 表示飞行单位
- `null` 表示该槽位无数据

---

*数据来源：100% 游戏内存直接读取 | 更新时间：2026-07-28*
